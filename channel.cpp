#include "channel.h"
#include <vector>
#include <complex>
using namespace std;

channel::channel(trxConfig& config){

}
vector<vector<complex<double> > > channel::fadingChannel(vector<vector<complex<double> > >& txsignal){

}